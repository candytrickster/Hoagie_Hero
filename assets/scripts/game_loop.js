function gameLoop()
{
    switch(game_state)
    {
        case state.TITLE:
            break;
        case state.INSTRUCTIONS:
            break;
        case state.PLAY:
            break;
        case state.GAME_OVER:
            break;
        default:
            break;
    }
}
